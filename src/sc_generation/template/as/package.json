{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "asc $SC_DIR/$SC_NAME.ts  --target release --outFile build/$SC_DIR/$SC_NAME.wasm",
    "build_windows": "asc %SC_DIR%/%SC_NAME%.ts --target release --outFile build/%SC_DIR%/%SC_NAME%.wasm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "assemblyscript": "^0.26",
    "typescript": "^4.8.4"
  }
}